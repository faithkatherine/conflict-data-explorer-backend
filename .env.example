# Production Environment Variables for Heroku

# Required for Heroku
NODE_ENV=production
PORT=$PORT

# Database (using SQLite for quick deployment)
DB_TYPE=sqlite
DB_PATH=./data/database.db

# JWT Secret (change this in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-2024

# CORS (add your frontend domain after deployment)
CORS_ORIGIN=http://localhost:3000,https://your-app-name.vercel.app

# Optional: Logging
LOG_LEVEL=info
